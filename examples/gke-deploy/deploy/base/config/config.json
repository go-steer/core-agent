{
  "_comment": "BASE recipe placeholder. Do not deploy this verbatim — model.vertex.project = 'PROJECT_ID' is a literal that won't resolve against any GCP project. Operators copy the example overlay and edit its own config/config.json; the overlay's configMapGenerator with behavior: replace swaps the entire ConfigMap with the operator's edited contents.",
  "version": 1,
  "model": {
    "provider": "vertex",
    "name": "gemini-3.1-pro-preview-customtools",
    "vertex": {
      "project": "PROJECT_ID",
      "location": "us-central1"
    }
  },
  "permissions": {
    "mode": "ask",
    "allow": [
      "read_file",
      "read_many_files",
      "stat",
      "list_dir",
      "glob",
      "grep",
      "json_query",
      "fetch_url",
      "todo"
    ]
  },
  "agent": {
    "display_name": "core-agent (GKE)"
  }
}
