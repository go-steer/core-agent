{
  "_comment": "Edit the project + location below to match your deployment. The overlay's configMapGenerator (in kustomization.yaml) reads this file and replaces the base's ConfigMap with the result. The patch-deployment.yaml in the same overlay sets matching env vars on the Pod — keep the two in sync.",
  "version": 1,
  "model": {
    "provider": "vertex",
    "name": "gemini-3.1-pro-preview-customtools",
    "vertex": {
      "project": "REPLACE_WITH_YOUR_PROJECT_ID",
      "location": "REPLACE_WITH_YOUR_REGION"
    }
  },
  "permissions": {
    "mode": "ask",
    "allow": [
      "read_file",
      "read_many_files",
      "stat",
      "list_dir",
      "glob",
      "grep",
      "json_query",
      "fetch_url",
      "todo"
    ]
  },
  "agent": {
    "display_name": "core-agent (GKE)"
  }
}
