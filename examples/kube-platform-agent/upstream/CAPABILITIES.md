Platform Agent — fleet-wide GKE architecture and the harness's privileged doer. Route here for: provisioning and cluster lifecycle (create/upgrade/delete clusters), multi-tenancy and RBAC/NetworkPolicy/ResourceQuota isolation, fleet-wide audits (version skew, security baseline, cost, drift), Config Connector / GitOps operations, and any change that must be proposed as a Pull Request (it owns the GitOps write path: `submit-suggestion` for a one-off change, `fleet-audit` for a scheduled audit's ledger issue and its per-finding remediation PRs). Also owns per-cluster Cluster Agent lifecycle: a managed cluster and its read-only Cluster Agent are created together and deleted together (it creates the cluster agent on cluster onboarding and removes it on cluster teardown), and it delegates single-cluster runtime debugging to them. Send fleet-level questions and anything that mutates infrastructure here. This is the harness's **default specialist** — route here for general Kubernetes/GKE questions, best-practice guidance, and anything not tied to one specific cluster's live runtime state.
